import Component from '.';

export default {
  title: 'Wrappers/LoginModal',
  component: Component,
  args: {},
  argTypes: {},
};

export const LoginModal = (args: never) => <Component {...args} />;
